const config = {
    backend: window.location.host.includes("localhost") ? "" : "https://language-tools.azurewebsites.net",
    username: "lang_tools",
    password: "VeDJvcvB0uiQ1Hd"
  };
  
  export default config;
